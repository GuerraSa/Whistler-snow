import utils.forecast as forecast
import utils.func as func

# --- EXECUTION ---
# You can now run all four:
forecast.get_forecast("1480m")
forecast.get_forecast("1800m")
forecast.get_forecast("2248m")
forecast.update_snow_history()